rootProject.name = 'saimiri'
include 'gfx'
include 'base'
include 'game'
include 'tmx'

def prefix(prefix) {
    rootProject.name = prefix
    rootProject.children.each { it.name = "${prefix}-${it.name}" }
}

prefix('saimiri')

include 'saimiri-tmx:TiledReader'